Day 12 = CALCULATE(
                    COUNTROWS(
                        FILTER(
                            SUMMARIZE(Orders, Orders[CustomerID], Orders[OrderDate]), YEAR(Orders[OrderDate]) = 2022)
                    ))
